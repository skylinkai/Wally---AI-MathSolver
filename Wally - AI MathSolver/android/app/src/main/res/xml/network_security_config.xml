<?xml version="1.0" encoding="utf-8"?>
<!-- Allows HTTP to local dev server (emulator 10.0.2.2 or LAN IP). Remove for production HTTPS-only. -->
<network-security-config>
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
